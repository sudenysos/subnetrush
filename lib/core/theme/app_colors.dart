import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF635BFF); // Deep Blurple
  static const Color primaryDark = Color(0xFF4B45C6);
  static const Color primaryLight = Color(0xFF8F89FF);

  // Secondary / Accent
  static const Color secondary = Color(0xFF00D4FF); // Teal/Cyan
  static const Color accent = Color(0xFF00D4FF);

  // Backgrounds
  static const Color background = Color(0xFFF6F9FC); // Off-white / Light Grey
  static const Color surface = Color(0xFFFFFFFF); // Pure White

  // Text
  static const Color textPrimary = Color(0xFF0A2540); // Dark Slate
  static const Color textSecondary = Color(0xFF425466); // Cool Grey
  static const Color textLight = Color(0xFF8898AA);

  // Status
  static const Color success = Color(0xFF3ECF8E);
  static const Color warning = Color(0xFFF6A723);
  static const Color error = Color(0xFFCD3D64); // Soft Red
  
  // UI Elements
  static const Color border = Color(0xFFE6EBF1);
  static const Color cardShadow = Color(0x1C32325D); // Subtle shadow color
}
